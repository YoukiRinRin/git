import pattern as pt
import re
import main

str = main.inp_str

#文字がドットで区切られている
dot = re.match(pt.dot,str)

kata = re.match(pt.kata,str)
kata_befor = re.match(pt.kata_befor,str)
kata_after = re.match(pt.kata_after,str)
kata_dot = re.match(pt.kata_dot,str)

dot = re.match(pt.dot,str)
dot = re.match()




